language: common-lisp
sudo: required

env:
  matrix:
    - LISP=sbcl

install:
  - curl https://raw.githubusercontent.com/luismbo/cl-travis/master/install.sh | bash

script:
  - cl -l rove -e '(or (rove:run :propositional-logic/tests) (uiop:quit -1))'
