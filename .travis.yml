language: common-lisp
sudo: required

env:
  matrix:
    - LISP=sbcl

install:
  - curl https://raw.githubusercontent.com/luismbo/cl-travis/master/install.sh | bash

script:
  - cl -l rove -e '(or (asdf:test-system :propositional-logic) (uiop:quit -1))'
